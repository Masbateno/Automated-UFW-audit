[profile]
name        = container
extends     = desktop
description = Container/CI environment — hardening section skipped, minimal footprint assumed

[skip_sections]
# Containers typically have no persistent package manager, no systemd,
# and no AppArmor profiles — hardening checks produce false positives.
hardening
